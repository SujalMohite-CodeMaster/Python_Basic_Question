# Write the python program that print the area of triangle.

base = abs(float(input("Enter the base of the triangle. ")))
height = abs(float(input("Enter the height of the triangle. ")))

area = 0.5 * base * height

print(f"The area of the triangle with base {base} and height {height} is {area}.")